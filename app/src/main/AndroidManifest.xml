<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codewithshadow.fampay">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FamPay"
        android:usesCleartextTraffic="true">
        <activity android:name=".activites.WebActivity"></activity>
        <activity android:name=".activites.ApiScreenActivity" />
        <activity android:name=".activites.DashboardActivity" />
        <activity android:name=".activites.AadhaarNumberActivity" />
        <activity android:name=".activites.HelloActivity" />
        <activity android:name=".activites.FamPayCardActivity" />
        <activity android:name=".activites.VerifyOTPActivity" />
        <activity android:name=".activites.LoginActivity" />
        <activity android:name=".activites.OnBoardingScreenActivity" />
        <activity android:name=".activites.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>